 const divElement = document.getElementById('myDiv');
        const username = 'Ahmed'; // افترض أن المستخدم هو إداري

        if (username === 'YASSER') {
            divElement.classList.add('user-admin');
        }
